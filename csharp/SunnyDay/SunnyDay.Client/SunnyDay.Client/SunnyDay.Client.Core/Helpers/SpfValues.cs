namespace SunnyDay.Client.Core.Helpers
{
    public enum SpfValues
    {
        // TODO: maybe change these values (no side effects)
        None = 0,
        Low = 15,
        Medium = 30,
        High = 40,
        Extreme = 50
    }
}